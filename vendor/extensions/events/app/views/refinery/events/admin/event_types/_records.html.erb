<%= render 'refinery/admin/search_header', :url => refinery.events_admin_event_types_path %>
<div class='pagination_container'>
  <% if @event_types.any? %>
    <%= render 'event_types' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
